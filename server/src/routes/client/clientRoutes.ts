import { FastifyInstance } from "fastify";

export const clienteRoutes = async (app: FastifyInstance) => {
    // // inicio - Dashboard
    // app.get("/userData", { onRequest: authUsuario }, usuariorController.getDadosUsuario)
}